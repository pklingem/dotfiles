name: rise-authoring
windows:
  - name: server
    root: ~/code/rise-authoring
    panes:
      - grunt clean && grunt
  - name: vim
    root: ~/code/rise-authoring
    panes:
      - vim
  - name: terminal
    root: ~/code/rise-authoring
    panes:
      - git status
