name: rise-authoring
windows:
  - name: server
    root: ~/src/articulate/rise-authoring
    panes:
      - commands:
        - grunt clean && grunt
  - name: vim
    root: ~/src/articulate/rise-authoring
    panes:
      - commands:
        - vim
  - name: terminal
    root: ~/src/articulate/rise-authoring
    panes:
      - commands:
        - git status
